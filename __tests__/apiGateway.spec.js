const nodeGateway = require('../server/nodeGateway');

describe('node gateway', () => {
  // test('should have a ', () => {   
  // })
  test('two plus two is four', () => {
    expect(2 + 2).toBe(4);
  });
});
